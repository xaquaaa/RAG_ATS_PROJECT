fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.10.0
python-dotenv==1.0.1
fastembed==0.8.0  # ONNX runtime, no PyTorch — swapped from sentence-transformers
                   # after Render's free 512MB tier ran out of memory with two
                   # PyTorch-backed models loaded simultaneously
groq==1.6.0
pypdf==4.3.1
streamlit
requests==2.32.3
numpy>=2.0.0

psycopg2-binary
pgvector==0.3.5

# Dev / tests
pytest==8.3.3
